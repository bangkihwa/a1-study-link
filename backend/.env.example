# A1 StudyLink LMS - 환경변수 설정 예시
# 이 파일을 복사해서 .env 파일로 만들고 실제 값으로 변경하세요.

# ===========================================
# 서버 설정
# ===========================================
NODE_ENV=development
PORT=3000

# ===========================================
# 데이터베이스 설정
# ===========================================
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=mysql
DB_NAME=a1_studylink
DB_PORT=3306

# ===========================================
# JWT 보안 설정
# ===========================================
# 운영환경에서는 반드시 복잡한 시크릿 키를 사용하세요
JWT_SECRET=your_very_secure_jwt_secret_key_here

# ===========================================
# 관리자 계정 설정
# ===========================================
# 최초 실행 시 자동으로 생성될 관리자 계정 정보
ADMIN_USERNAME=admin
ADMIN_PASSWORD=SecureAdminP@ss2024!
ADMIN_NAME=시스템 관리자
ADMIN_EMAIL=admin@a1studylink.com

# ===========================================
# 프론트엔드 설정
# ===========================================
FRONTEND_URL=http://localhost:3000

# ===========================================
# 보안 설정 (선택사항)
# ===========================================
# CORS_ORIGIN=http://localhost:3000
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# 파일 업로드 설정 (선택사항)
# ===========================================
# UPLOAD_MAX_SIZE=10485760
# UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,application/pdf

# ===========================================
# 이메일 설정 (향후 구현)
# ===========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password